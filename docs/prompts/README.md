# Prompts

Purpose:

Copy-paste-ready Claude Code prompts, one per Step, matching the numbering in `../implementation/`. Every prompt follows the Context / Objective / Constraints / Expected Output structure defined in `../../AI-QA-OS-Documentation-Standard.md` Section 6.

Naming convention:

```
NN-Claude-Prompt.md
```

Example (once authored):

```
01-Claude-Prompt.md
02-Claude-Prompt.md
03-Claude-Prompt.md
```

Status:

No files yet. The first file to create is `01-Claude-Prompt.md`, from `../templates/Claude-Prompt-Template.md`, once `../implementation/01-Requirement-Management.md` is scoped.

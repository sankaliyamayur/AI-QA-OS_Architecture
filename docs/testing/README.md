# Testing

Purpose:

Test strategy and coverage documentation for the AI-QA-OS platform itself (how *it* is tested — Unit / Integration / System / AI Validation Testing, per `00-Foundation/01_PROJECT_VISION.md` "Testing Strategy"). Distinct from `verification/`, which records pass/fail evidence per implementation Step.

Naming convention:

Descriptive, no number required — small fixed set of documents.

Example (once authored):

```
Test-Strategy.md
Coverage-Report.md
```

Status:

No files yet. `Test-Strategy.md` should be the first file, written once Phase 3 (Common Infrastructure) establishes the testing setup.

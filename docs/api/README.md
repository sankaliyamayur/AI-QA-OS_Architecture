# API

Purpose:

API reference documentation, added as REST/GraphQL endpoints are actually built (starting around Phase 6 - Agent Generation / Phase 8 - Integration Setup, per `../roadmap/00-Project-Roadmap.md`).

Status:

No files yet — no API surface exists to document. Populate this folder once the first endpoint is implemented, one file per API resource/module.

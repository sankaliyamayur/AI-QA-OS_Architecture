# Architecture

Purpose:

Architecture notes scoped to an individual implementation Step or subsystem — system diagrams, module dependency maps, sequence diagrams, deployment diagrams. This complements, and cross-references rather than duplicates, the platform-wide architecture already defined in `../../00-Foundation/`.

Naming convention:

```
NN-Descriptive-Name.md
```

Example (once authored):

```
01-System-Architecture.md
02-Module-Dependencies.md
```

Status:

No files yet. The first file will be created alongside Step 01 once its implementation doc exists — see `../roadmap/00-Project-Roadmap.md`.

Template: `../templates/Architecture-Template.md`

# Examples

Purpose:

Worked, end-to-end examples — a sample requirement carried through scenario, test case, test data, and automation generation — used to sanity-check that a Step behaves as documented. Distinct from `08-Generated/` in the platform (real generated output) and from `14-Examples/` in `00-Foundation/`'s Repository Construction Blueprint (reference implementations of platform components) — this folder holds documentation-level walkthrough examples.

Naming convention:

Descriptive, no number required.

Example (once authored):

```
Login-Flow-Example.md
```

Status:

No files yet. Add a worked example once at least one full Requirement → Automation chain has been implemented and verified.

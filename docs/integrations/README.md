# Integrations

Purpose:

How-to and configuration reference docs for connecting AI-QA-OS to external systems — GitHub, Jira, Jenkins, MCP servers, cloud services — as they're actually wired up (see `00-Foundation/00_FOUNDATION_BLUEPRINT.md` — MCP Integration Blueprint for the architecture; this folder documents the setup/usage side).

Naming convention:

Descriptive, no number required — one file per integration.

Example (once authored):

```
GitHub-MCP-Setup.md
Jira-Integration.md
Playwright-MCP-Setup.md
```

Status:

No files yet. Add a file the first time an integration from Phase 8 (Integration Setup) is actually configured.
